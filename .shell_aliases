# Put this in your home folder
# 1. create ~/.shell_aliases with this content:
# 2. check if your shell config (often ".???rc") includes this by using ". ~/.shell_aliases"

#Aliases for improved use of directory listings
#The --color=tty option shows items in different colors according to their type.
#For example, directories are blue, executable files are green, symlinks are cyan, etc.
#The -F option appends a symbol after entries to indicate their types.
#You might not like both options at the same time.
alias ls='ls -F --color=tty' #regular ls
alias l.='ls -dF .[a-zA-Z0-9]* --color=tty' #only show dotfiles
alias ll='ls -lhF --color=tty' #long listing
alias la='ls -AhF --color=tty' #all files even with dot at beginning

# Use of file system crud
alias md='mkdir' #make directory
alias cp='cp -r' #easier to copy directories

#Make these commands ask before clobbering a file. Use -f to override.
#alias rm="rm -i"
#alias cp="cp -i" #overwrites the above
#alias mv="mv -i"

#Use human-readable filesizes
alias du="du -h"
alias df="df -h"

#Miscellaneous
#alias bzip2='bzip2 -v'
alias j=jobs
alias cd..="cd .." # work around a common typo

#Automatically do an ls after each cd
cd() {
  if [ -n "$1" ]; then
    builtin cd "$@" && ls
  else
    builtin cd ~ && ls
  fi
}

#
# Alias stuff
#
urlencode() {
  echo -n $* | od -A n -t x1 | sed 's/ /\%/g'
}

# Search google for the given keywords.
function google {
        $BROWSER "http://www.google.com/search?q=$( urlencode $* )"
}
